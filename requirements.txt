# PatriotGames — fantasy basketball toolkit (data_feed, optimizer, FastAPI)

# ESPN & data layer
espn-api>=0.19.0
numpy>=1.24.0
pandas>=2.0.0
python-dateutil>=2.8.0
pytz>=2023.3
rapidfuzz>=3.0.0
requests>=2.28.0

# Excel (pandas read_excel / to_excel)
openpyxl>=3.1.0
xlrd>=2.0.0

# Draft optimizer
cvxpy>=1.4.0
shutup>=0.2.0

# FastAPI service
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Anthropic Claude (matchup commentary)
anthropic>=0.0.0

# Repair almost-valid JSON from the LLM structured-recap call (unescaped quotes etc.)
json_repair>=0.30.0

# FastAPI multipart/form-data support (File/Form)
python-multipart>=0.0.9

# Loads `.env` into environment variables (used by backend.api.main for ANTHROPIC_API_KEY)
python-dotenv>=1.0.0

# Tests
pytest>=7.0.0
